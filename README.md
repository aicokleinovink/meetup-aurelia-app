### Apeldoorn WebDevs Meetup Aurelia Demo App

Install `aurelia-cli` globally if you like:
```
$ npm install -g aurelia-cli
```

Install the application dependencies:
```
$ npm install
```

Run the application (`--watch` enables live reload):
```
$ au run --watch
```
